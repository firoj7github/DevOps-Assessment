
apiVersion: v1
kind: Secret
metadata:
  name: hydrus-db-secret
  namespace: hydrus
type: Opaque
data:
  # base64-encoded values (echo -n 'value' | base64)
  DB_USER: aHlkcnVz         # hydrus
  DB_PASSWORD: CHANGEME==   # REPLACE with real base64 value